1. 입력 : 문자 1개가 입력된다
2. 출력 : 그 다음 문자를 출력한다.

a = input()
a = ord(a)
print(chr(a+1))

풀이 :
1) a = input() 으로 문자열 1개를 a에 입력
2) a = ord(a) 으로 문자1개의 유니코드(숫자타입) a에 입력
3) chr(a+1)으로 유니코드 + 1(다음 값에 해당함)을 문자로 변환해서 출력
