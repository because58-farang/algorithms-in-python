1. 문제 
행복 왕국의 왕실 정원은 체스판과 같은 8 × 8 좌표 평면이다. 왕실 정원의 특정한 한 칸에 나이트가 서 있다. 나이트는 매우 충성스러운 신하로서 매일 무술을 연마한다.
나이트는 말을 타고 있기 때문에 이동을 할 때는 L자 형태로만 이동할 수 있으며 정원 밖으로는 나갈 수 없다. 나이트는 특정한 위치에서 다음과 같은 2가지 경우로 이동할 수 있다.

- 수평으로 두 칸 이동한 뒤에 수직으로 한 칸 이동하기
- 수직으로 두 칸 이동한 뒤에 수평으로 한 칸 이동하기

  a  b  c  d  e  f  g  h
1
2
3
4
5
6
7
8

이처럼 8 × 8 좌표 평면상에서 나이트의 위치가 주어졌을 때 나이트가 이동할 수 있는 경우의 수를 출력하는 프로그램을 작성하시오. 
이때 왕실의 정원에서 행 위치를 표현할 때는 1부터 8로 표현하며, 열 위치를 표현할 때는 a부터 h로 표현한다.

2. 입력
- 첫째 줄에 8x8 좌표 평면상에서 현재 나이트가 위치한 곳의 좌표를 나타내는 두 문자로 구성된 문자열이 입력된다.
- 입력 문자는 a1 처럼 열과 행으로 이뤄진다.

3. 출력
- 첫째 줄에 나이트가 이동할 수 있는 경우의 수를 출력하시오.

4. 해결
input_data = input()
row = int(input_data[1])
column = ord(input_data[0]) - ord('a') + 1

result = 0
steps = [(-2,-1), (-1,-2),(1,-2),(2,-1),(-2,1),(-1,2),(1,2),(2,1)]

for step in steps:
  next_row = row + step[0]
  next_column = column + step[1]

  if next_row<1 or next_column<1 or next_row>8 or next_column>8:
    continue
  result += 1

print(result)

5. 풀이
  1) 입력받는 2자리 문자열 중 첫 번째는 문자이므로 유니코드 변환(ord 함수)를 사용해야한다(연산 중간중간마다 ord 함수 사용하는 것을 피할 수 있음)
  -> ord(문자열) -ord('a') +1 을 하면 column(열)의 좌표를 구할 수 있다.(+1을 하면 1부터 시작)
  2) 나이트가 이동 가능한 경우의 수들을 steps 변수에 담는다
  3) 2차리스트 steps 내부를 for문으로 돌림으로써 column과 row의 다음 이동 위치들을 구한다
  4) 3번에서 구해진 이동 위치들이 8x8 평면을 벗어나는지 확인한다.

6. 기타
- 처음에 생각했던 건 
나이트가 이동 가능한 경우의 수 8가지(아래)를 각각 if문으로 구현하여 각각을 입력받은 row와 column 값과 비교하여 8x8 평면을 벗어나지 않으면 reult 를 더하도록 하려고 했으나
코드가 길어지고, 같은 코드를 계속 작성하는 것 같아서 풀이를 보고 구현
(1)data[0]+2 와 입력받은 column의 합 and data[1]+1 와 입력받은 row의 합
(2)data[0]+2 와 입력받은 column의 합 and data[1]-1 와 입력받은 row의 합
(3)data[0]+1 와 입력받은 column의 합 and data[1]+2 와 입력받은 row의 합
(4)data[0]+1 와 입력받은 column의 합 and data[1]-2 와 입력받은 row의 합
(5)data[0]-1 와 입력받은 column의 합 and data[1]+2 와 입력받은 row의 합
(6)data[0]-1 와 입력받은 column의 합 and data[1]-2 와 입력받은 row의 합
(7)data[0]-2 와 입력받은 column의 합 and data[1]+1 와 입력받은 row의 합
(8)data[0]-2 와 입력받은 column의 합 and data[1]-1 와 입력받은 row의 합
